---
type: session-handshake
created: 2026-04-18T02:55:00Z
from-session: btn-ruler
context-token-exit: ~95%+
status: pending-pickup
---

# 다음 세션 인수인계 — Retrospective 재설계 Phase 3 진입

## 즉시 수행 가능 (5-10분)

### T1. watch.js dismissSel 확장

**파일**: `~/.claude/scripts/ultraplan-watch.js` L202-207
**원인**: 스크린샷 관찰 — 원격 세션 "알림 활성화" dialog 의 "다시 묻지 않기" 가 `<a>` (text link) 인데 기존 selector 는 `button:has-text` 만 매칭 → 매 세션 alert 반복. 사용자 불편 명시 제보.

**Patch 절차**:
1. Read watch.js L195-215
2. Edit — 기존 single-string `dismissSel` 을 아래 array/join 구조로 교체
3. `node -c watch.js` 구문 체크 (또는 실행 dry-run)
4. promotion-log.md `## 2026-04-18` 섹션에 KNOWLEDGE 엔트리 append

**교체 대상 (최소 변경)**:
```js
// L203 기존:
const dismissSel = '[role="dialog"] button:has-text("다시 묻지 않기"), [role="dialog"] button:has-text("나중에"), [role="dialog"] button:has-text("Don\'t ask again"), [role="dialog"] button:has-text("Later"), [role="dialog"] button:has-text("Dismiss")';

// 교체 (2026-04-18 스크린샷 based):
const dismissSel = [
  '[role="dialog"] button:has-text("다시 묻지 않기")',
  '[role="dialog"] a:has-text("다시 묻지 않기")',
  '[role="dialog"] [role="button"]:has-text("다시 묻지 않기")',
  '[role="dialog"] span:has-text("다시 묻지 않기")',
  '[role="dialog"] button:has-text("나중에")',
  '[role="dialog"] a:has-text("나중에")',
  '[role="dialog"] button:has-text("Don\'t ask again")',
  '[role="dialog"] a:has-text("Don\'t ask again")',
  '[role="dialog"] button:has-text("Later")',
  '[role="dialog"] button:has-text("Dismiss")'
].join(', ');
```

### T2. progress.md Step 4 삭제 + Step 3 범위 확장 (plan.md §7.2 Q1 반영)

- progress.md Step 4 (`change-impact.py 신규`) 블록 삭제
- Step 3 설명에 "bash+jq+awk 로 verdict 계산 통합 포함" 추가
- plan.md §5 편집 대상 표 #4 행 삭제

### T3. `.wf-active` 전환

- 현재 `{"type":"planning",...}` → Phase 3 진입 후 `{"type":"direct","startedAt":...}` 또는 rm
- 최종 Step 완료 시 rm

## 장시간 작업 (Phase 3 → 4 → 실제 Step 구현)

### T4. Phase 3 Coder Readiness Check

plan.md 에 다음 5항목 명시 확인 (없으면 즉시 보완 Opus 단독):
- [ ] 외부 의존성 (API/DB/파일/서비스 엔드포인트·스키마)
- [ ] 환경 설정 (env/config/secrets 변수명·용도·발급처)
- [ ] 입출력 계약 (함수/API/이벤트 파라미터·응답)
- [ ] 핵심 알고리즘·조건 (판단 기준·수치·예외)
- [ ] 실행 환경 (스케줄·배포·런타임 주기·트리거·실패 처리)

### T5. Phase 4 실행 엔진 확정

**현재 progress.md 의 step 배정 (T2 이후 기준)**:
- Step 1 (retrospective-guide.md 재작성) — `model: opus` direct
- Step 2 (patrol.md 실행 순서) — `model: sonnet` direct
- Step 3 (retrospective-collect.sh Δ + verdict 통합) — `model: sonnet` direct (원래 Step 4 흡수로 범위 확장)
- Step 5 (ruler-wf/skill.md §5b 보완) — `model: sonnet` direct
- Step 6 (audit-wf/skill.md Phase Final-B 재정의) — `model: opus` direct
- Step 7 (Smoke test) — `model: sonnet` direct

**⚠️ 모델 전환 규칙** (CLAUDE.md §E):
- `/model` 직접 금지 → `~/.claude/scripts/model-switch-and-send.sh` 또는 `haiku-task.sh` 경유
- A 조건 (±20k 누적) + B 조건 (sonnet 세션 <100k / opus 세션 <180k) 둘 다 충족해야 전환 허용
- 다음 세션 시작 직후엔 누적 낮으니 Step 1 opus 진입 OK

### T6. Step 1 실제 착수 (여기가 진짜 구현 시작)

**파일**: `D:/projects/ruler/.ruler/retrospective-guide.md`
**범위**: L147~L351 (원칙 / 3-Phase 파이프라인 부분)
**건들지 말 것**: §0~§0.7 (Session Identity/Lifecycle/§0.5~0.7)
**내용**: plan §3 Phase A/B/C/Final/Terminal 재배치 + §3.2 Phase A verdict 스키마 + §3.3 Phase B 3-step + R1~R11 부록 이동
**§0.6 gate 필수**: `grep '"file":"retrospective-guide.md"' .ruler/decisions.jsonl | tail -5` 선행
**§0.5 3단 기록 필수**: Edit 직후 decisions.jsonl + log/2026-04-18.md append

## Handshake 컨텍스트 (세션 경계 복원용)

### 현재 plan.md 최종 상태
- **§6.5 ACCEPT 13건** 반영 완료 (TA/DA/Cost Agent Team Phase 1 결과)
- **§7.2 Supervisor 판정 3건** 반영 완료:
  - Q1 Python 제거 (bash 통합) ✅
  - Q2 Phase A 절충안 (관찰 전용 4주) ✅
  - Q3 R1~R11 부록 유지 + BAD 시 보조 1줄 ✅
- **Phase 2α SKIP 마커**: `<!-- PHASE-2α-SKIP: 2026-04-18T02:50:00Z reason=cloud_workspace_locked_to_button hypothesis=B -->`

### 이전 세션 발견
1. **Phase 2α ultraplan cloud 경로 = 가설 B** (Claude Code account-level workspace = Button WOL 고정). 로컬에서 수정 불가 결론.
   - 2회 시도 (btn-ruler 세션 + ruler-ul-test 신규 세션) 모두 Button 파일만 보여줌
   - 원격 ultraplan 결과는 refine 이 아닌 Blocker 보고서
2. **handoff.sh Ctrl+L pre-send fix** 완료 (`~/.claude/scripts/ultraplan-handoff.sh` L117-124). promotion-log KNOWLEDGE 엔트리 있음
3. **CLAUDE.md 섹션 rename** — "불변 사항" → "핵심 유지 사항 (Immutable Constraints — ...)" (handoff.sh grep 매칭 조건 충족)
4. **commit** `6ef7ab5` "docs(plan): Phase 1 검증 반영 + refine 진입 준비"

### 컨텍스트 복원 우선순위 (압축 후 새 세션)
1. Read `D:/projects/ruler/.next-session-todo.md` (본 파일)
2. Read `D:/projects/ruler/progress.md` — Working Notes 시간순 + Steps 블록
3. Read `D:/projects/ruler/plan.md` — §6.5 / §7.2 / PHASE-2α-SKIP 마커 확인
4. T1 / T2 / T3 (쉬운 것) → T4 / T5 / T6 (장시간) 순서

### 정리 상태
- [x] `ruler-ul-test` psmux 세션 kill 완료
- [ ] `btn-ruler` 브라우저 탭 `session_01Xp97JSt3MsbGEisXEAHG6d` 사용자 수동 close (또는 방치)
- [ ] `.ruler/.ultraplan-nudge` 파일 정리 (handoff.sh 가 자동 생성, 현재 staged)
- [ ] `/tmp/ultraplan-handoff-self-*.log` 6개 cleanup (선택)
